process {
    executor = 'local'
    cpus = 2
    memory = '4 GB'
    time = '2h'

    withName: MATCH_AND_MOTIFS {
        cpus = 4
        memory = '8 GB'
    }
}

docker.enabled = true  // Set false to use Conda
